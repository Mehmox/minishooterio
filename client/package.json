{
  "name": "react",
  "description": "minishooterio client + server.",
  "author": "Mehmox",
  "license": "Non-Commercial Use License (NCUL) – Version 1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ii": "npm i && npm i --prefix react",
    "build": "npm run build --prefix react && cpy \"react/build/**\" \"build/\" --overwrite && rimraf react/build",
    "r": "rimraf node_modules react/node_modules",
    "i": "npm i --production",
    "dev": "npm run dev --prefix react",
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "cpy": "^11.1.0",
    "rimraf": "^6.0.1"
  }
}